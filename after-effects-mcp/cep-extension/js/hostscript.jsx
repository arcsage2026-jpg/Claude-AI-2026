// Loaded into After Effects' ExtendScript engine when the MCP Bridge panel
// starts. The bridge (js/bridge-server.js) builds and sends complete,
// self-contained scripts per request via CSInterface.evalScript rather than
// calling functions defined here, so this file intentionally stays empty —
// it exists only because CEP's manifest requires a ScriptPath.
